services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: planmyroute-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - apps/backend/.env
